const div = { marginLeft: '8%' };
const image = { width: '68%', height: 720,marginTop:'4%', marginLeft: '-10%' }
const text = { color: '#64748B', fontSize: 14, fontStyle: 'normal', marginLeft: '-34%',marginTop:'1%' }
const rememberme = { marginTop: '1.5%', flexDirection: 'row' }
const checkbox = { marginLeft: '-44%' }
const forgot = { color: 'blue', textDecoration: 'none', marginRight: '-25%',marginTop:'-10%' }
const textAccount = { marginLeft: '6%', fontSize: 14 }
const linkSign={ color: 'blue', textDecoration: 'none' }
const buttonStyle={
    width: '50%',
    padding: '2%',
    marginTop: '2%',
    marginRight: '8%',
    borderRadius: 14,
    backgroundColor: '#0F585E',
    color: 'white'
  }
const paperStyle = {
  padding: 30,
  width: '65%',
  // margin: '0 auto',
  height:795,
  //  marginTop: '-1%',
  marginLeft: '-12%',
  marginBottom: '-5%',
  backgroundColor: '#F5F5F5'
  };
  const papernewStyle = { width: '58%',height:400,marginTop:'-2%',marginLeft:'-7%'};
  const textStyle = { width: '50%', marginTop: '4%', marginRight: '10%' };
  const headerStyle = {
    margin: 0,
    fontSize: 24,
    fontStyle: 'normal',
    marginTop: '10%',
    marginLeft: '-30%',
    borderRadius: 12,
    fontWeight: 700
};

const termsError = { color: 'red', fontFamily: 'sans-serif', fontSize: 12, marginRight: '18%' }
const passwordHeader={
    margin: 0,
    fontSize: 24,
    fontStyle: 'normal',
    marginTop: '15%',
    borderRadius: 12,
    fontWeight: 700,
    marginLeft:'9%'
};

export default {
    div,
    image,
    text,
    textAccount,
    rememberme,
    checkbox,
    forgot,
    linkSign,
    buttonStyle,
    paperStyle,
    papernewStyle,
    textStyle,
    headerStyle,
    termsError,
    passwordHeader
}