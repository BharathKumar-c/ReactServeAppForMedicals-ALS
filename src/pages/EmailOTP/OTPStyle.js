const div = { marginLeft: '8%' };
const image = { width: '68%', height: 720,marginTop:'4%', marginLeft: '-10%' }
const text = { color: '#64748B', fontSize: 14, fontStyle: 'normal', marginRight: '18%' }
const rememberme = { marginTop: '1.5%', flexDirection: 'row' }
const checkbox = { marginLeft: '9%' }
const forgot = { color: 'blue', textDecoration: 'none', marginLeft: '26%' }
const textAccount = { marginLeft: '6%', fontSize: 14 }
const linkSign={ color: 'blue', textDecoration: 'none' }
const buttonStyle={
    width: '65%',
    padding: '2%',
    marginTop: '2%',
    marginLeft: '10%',
    borderRadius: 14,
    backgroundColor: '#0F585E',
    color: 'white'
  }
const paperStyle = {
     padding: 30,
    width: '65%',
    // margin: '0 auto',
    height:795,
    //  marginTop: '-1%',
    marginLeft: '-12%',
    marginBottom: '-5%',
    backgroundColor: '#F5F5F5'
  };
  const papernewStyle = { width: '58%',height:400,marginTop:'-2%',marginLeft:'-7%'};
  const textStyle = { width: '50%', marginTop: '4%', marginLeft: '10%' };
  const headerStyle = {
    margin: 0,
    fontSize: 24,
    fontStyle: 'normal',
    marginTop: '15%',
    marginLeft: '-10%',
    borderRadius: 12,
    fontWeight: 700
};

const termsError = { color: 'red', fontFamily: 'sans-serif', fontSize: 12, marginRight: '18%' }
const passwordHeader={
    margin: 0,
    fontSize: 24,
    fontStyle: 'normal',
    marginTop: '15%',
    borderRadius: 12,
    fontWeight: 700,
    marginLeft:'9%'
};
const button = {
    width: '45%',
    padding: '2%',
    marginTop: '9%',
    marginLeft: '-5%',
    borderRadius: 14,
    backgroundColor: '#0F585E',
    color: 'white'
};
const timerCount = { marginTop: '-10%', color: 'grey' };
const timerAlign = { marginLeft: '55%', marginBottom: '-2%' };
const resendStyle = {
    color: 'blue',
    fontSize: 10,
    opacity: 1,
    marginLeft: '20%',
    marginTop: '-2%'
};
const errorEmail={ color: 'red', margin: 5 ,fontSize:10,marginTop:'2%'}
const resendDisable = {
    color: 'blue',
    fontSize: 10,
    opacity: 0.5,
    marginLeft: '20%',
    marginTop: '-2%'
};
const sentCodemsg = { fontSize: 10, marginTop: '3%' }
const emailStyle = { color: 'blue', marginLeft: '20%', marginTop: '-2%' }
const didntReceive={ fontSize: 10, marginTop: '2%' }

export default {
    div,
    image,
    text,
    textAccount,
    resendDisable,
    didntReceive,
    emailStyle,
    errorEmail,
    resendStyle,
    sentCodemsg,
    rememberme,
    checkbox,
    timerAlign,
    forgot,
    linkSign,
    buttonStyle,
    timerCount,
    paperStyle,
    papernewStyle,
    textStyle,
    headerStyle,
    termsError,
    passwordHeader,
    button
}